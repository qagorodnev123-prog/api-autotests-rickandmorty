# Rick and Morty API Automation Framework

Автоматизированный фреймворк для тестирования публичного API Rick and Morty. Проект построен на современном стеке Python и реализует профессиональный подход к архитектуре автотестов.

## 🚀 Стек технологий
* Python 3.13
* Pytest — основной фреймворк тестирования.
* HTTPX — современный клиент с поддержкой Event Hooks.
* Pydantic v2 — строгая валидация схем ответов.
* Allure Report — интерактивные отчеты о тестировании.
* Faker — генерация динамических тестовых данных.

## 🏗 Архитектура проекта
Проект организован по многослойному принципу (Layered Architecture):
* clients/ — Инкапсуляция HTTP-запросов. Реализован APIClient с поддержкой curl_event_hook.
* schemas/ — Pydantic-модели для автоматической проверки структуры и типов данных.
* tools/assertions/ — Библиотека проверок. Включает универсальные ассерты (base_assertions) и специфичные проверки.
* fixtures/ — Модульные фикстуры Pytest для управления клиентами и данными.
* constants/ — Словари и перечисления (Enum) для статус-кодов и параметров.

## 🌟 Ключевые фичи
1. cURL в отчетах: Благодаря кастомному хуку в httpx, каждая выполненная команда автоматически прикрепляется к Allure-отчету.
2. Универсальный валидатор: Реализован метод check_unique_id, позволяющий проверять уникальность сущностей (ID) в ответах.
3. Глубокая валидация ссылок: Тесты проверяют не только наличие ссылок, но и их работоспособность через get_request.
4. Тегирование: Использование AllureTag позволяет фильтровать запуски (Regression, Negative, Smoke).

## 🛠 Быстрый старт

1. Клонирование и настройка:
```
git clone https://github.com/qagorodnev123-prog/api-autotests-rickandmorty
cd rick_and_morty_api_tests
python -m venv venv
```

2. Активация окружения:
```
Для Windows: venv\Scripts\activate
Для Linux/macOS: source venv/bin/activate
```

3. Установка и запуск:
```
pip install -r requirements.txt
pytest --alluredir=allure-results
allure serve allure-results
```

## 📈 Структура тестов
* Characters: Проверка списков, фильтрации, валидация 404 и уникальности ID.
* Locations: Проверка структуры локаций и доступности ссылок на резидентов.
* Episodes: Валидация данных эпизодов и связанных персонажей.

---
Проект выполнен в рамках обучения автоматизации тестирования на Python
